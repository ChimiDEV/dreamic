export enum MonadType {
  Just = 'Maybe(Just)',
  Nothing = 'Maybe(Nothing)',
  Left = 'Either(Left)',
  Right = 'Either(Right)',
}

export enum MonadTag {
  Just = 'Just',
  Nothing = 'Nothing',
  Left = 'Left',
  Right = 'Right',
}
